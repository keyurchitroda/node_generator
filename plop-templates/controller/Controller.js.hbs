const db = require("../model")
const Op = db.Sequelize.Op


const get{{ pascalCase name }} = async (req,res) => {
  try{
      console.log(req.body)
  }catch(err){
    console.log(`${{pascalCase name}}() - Error`, err)
  }
};

module.exports ={
  get{{pascalCase name}}
}